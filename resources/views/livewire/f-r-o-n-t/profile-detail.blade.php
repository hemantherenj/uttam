<div class="pt-24">
    <div class="container mx-auto py-12">
        <h2 class="text-lg font-semibold">{{ $detail->first_name }}</h2>
        <p class="text-gray-600 text-sm">{{ $detail->dob }}</p>
        <p class="text-gray-600 text-sm">{{ $detail->profile_for }}</p>
        <p class="text-gray-600 text-sm">{{ $detail->profile_for }}</p>
        <p class="text-gray-600 text-sm">{{ $detail->profile_for }}</p>
        <p class="text-gray-600 text-sm">{{ $detail->profile_for }}</p>
        <p class="text-gray-600 text-sm">{{ $detail->profile_for }}</p>
        <p class="text-gray-600 text-sm">{{ $detail->profile_for }}</p>
        <p class="text-gray-600 text-sm">{{ $detail->profile_for }}</p>
        <p class="text-gray-600 text-sm">{{ $detail->profile_for }}</p>
    </div>
</div>
